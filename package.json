{
  "name": "tab-clamp",
  "version": "0.1.3",
  "private": true,
  "description": "Browser extension that clamps you into a chosen set of tabs for a set time.",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node build.js",
    "package": "node build.js --package",
    "check": "node --check src/background.js && node --check src/content.js && node --check src/popup.js && node --check src/exit.js"
  }
}
